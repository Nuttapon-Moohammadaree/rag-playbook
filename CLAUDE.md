## แนวทางให้ Claude Code ใช้งานหลายคอร์ (Parallelism)

ให้พยายามทำงานที่กิน CPU/IO แบบ **parallel** เสมอ โดยใช้จำนวนคอร์ของเครื่อง (`$(nproc)`) และ/หรือโหมด auto-parallel ของแต่ละเครื่องมือ

### คำสั่งแนะนำ (ใช้ตามบริบท)

- **make**:
  - `make -j"$(nproc)"`
- **ninja**:
  - `ninja -j"$(nproc)"`
- **ripgrep (rg)**:
  - `rg --threads "$(nproc)" "PATTERN" PATH`
- **pytest (ถ้ามี pytest-xdist)**:
  - `pytest -n auto`
- **bun**:
  - `bun test --concurrency "$(nproc)"` (ถ้าเหมาะกับโปรเจกต์/เทส)

### ค่าเริ่มต้นที่ควรตั้งก่อนรันงาน (ถ้าไม่กระทบระบบ)

- ตั้ง `MAKEFLAGS` สำหรับ session นั้น ๆ:
  - `export MAKEFLAGS="-j$(nproc)"`

### หมายเหตุ

- เป้าหมายคือให้ **คำสั่งที่ Claude เรียกใช้** รันหลายคอร์ (build/test/search/index) ไม่ใช่ “เพิ่มคอร์ให้โมเดล” เพราะการตอบของโมเดลรันที่ฝั่งเซิร์ฟเวอร์เป็นหลัก



<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Jan 20, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #37 | 4:31 PM | 🔵 | Located VisionConfig Interface for Model Update | ~293 |
| #35 | " | 🔵 | Located Configuration Summary Section | ~237 |
| #34 | 4:30 PM | 🔵 | Located Image & OCR Processing Section | ~250 |
</claude-mem-context>